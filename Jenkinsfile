pipeline {
    agent any

    stages {
        stage('Build stage') {
            steps {
                echo 'Building project…'
            } 
        }
         stage('Deploy stage') {
            steps {
                echo 'Deploy project…'
            }
         }
          stage('Test stage') {
            steps {
                echo 'Test project…'
            }
        }
   }
}
